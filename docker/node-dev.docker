FROM rift-forum-node-base

# Add development tools
RUN npm install -g nodemon

# Add just the package file and install
ADD ./package.json /app/package.json
RUN npm install

# Start
ENTRYPOINT [ "nodemon", "npm", "start" ]
